{
  "name": "Fidelis Strategy Website",
  "install": "npm ci && (command -v sshpass >/dev/null && command -v rsync >/dev/null || (sudo apt-get update && sudo apt-get install -y --no-install-recommends sshpass rsync))",
  "terminals": [
    {
      "name": "next-dev",
      "command": "npm run dev",
      "description": "Next.js dev server for the Fidelis Strategy marketing site (http://localhost:3000)."
    }
  ],
  "ports": [
    {
      "name": "web",
      "port": 3000
    }
  ]
}
